// boolean
const usuarioLogado = true;
const contaPaga = false;

// truthy ou falsy

// 0 => false
console.log(0 == false);

// 1 => true
console.log(0 == true);

// "" => false
console.log("" == false);

// null => vazio ou nada
let minhaVarNull = null; // Typeof da variavel null retorna um objeto por que inicialmente foi planejada para tal -> nao influencia atualmente

// undefined => valor indefinido
let minhaVar;