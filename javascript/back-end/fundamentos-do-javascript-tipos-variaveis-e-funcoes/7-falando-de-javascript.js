// tipagem dinâmica

let minhaVar = 567;
minhaVar = 'texto';
minhaVar = true;

// Multiparadigma -> Varias formas para resolver o mesmo problema

// ES6 ou ES2015 -> Versao mais famosa do JavaScript -> entrou varias funcionalidades novas

// Linguagem interpretada -> O nosso codigo vai ser executado na mesma hora (JavaScript)
// Linguagem compilada -> O nosso codigo vai passar por um complicador e depois ser executado

// O que é o NodeJS -> É um interpretador de JavaScript backend