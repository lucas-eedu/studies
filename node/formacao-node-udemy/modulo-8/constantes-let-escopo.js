// "Variaveis" que não podem mudar de valor
const nome = 'Lucas';

// Diferanças let e var é o escopo

// let -> escopo global, local e bloco
// var -> escopo global, local

// bloco tudo entre {}
if (nome == 'Lucas') {

}